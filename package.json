{
  "name": "pouchdb-purge-demo",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "serve --symlinks",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serve": "^13.0.2"
  },
  "dependencies": {}
}
